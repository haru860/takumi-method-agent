---
name: takumi-method-core-all-only-text-data
description: 匠Methodのコアモデルのテキストデータを全て作成する。
---

# 実行手順

**前提条件**:
- `input/idea.md` が存在する

**【重要】以下のPhaseに従い実行してください。他のコマンドを参照して手順、ToDoリストを作る必要はありません。**

# Phase1 ステークホルダーモデルの作成

Skill toolを使用して `stakeholder-all-only-text-data` を実行する。

# Phase2 価値デザインモデルの作成

Skill toolを使用して `value-design-all-only-text-data` を実行する。

# Phase3 価値分析モデルの作成

Skill toolを使用して `value-analysis-all-only-text-data` を実行する。

# Phase4 要求分析ツリーの作成

Skill toolを使用して `requirement-tree-all-only-text-data` を実行する。

## 全体完了時の動作

Skill toolを使用して `common-procedures` を実行し、「完了報告」を行う。


