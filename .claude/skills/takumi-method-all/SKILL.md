---
name: takumi-method-all
description: 匠Methodの全成果物（テキストデータ + DrawIOモデル）を一括作成する。
---

# 実行手順

**前提条件**:
- `input/idea.md` が存在する

**【重要】以下の手順に従い実行してください。他のコマンドを参照して手順、ToDoリストを作る必要はありません。**

# Step1 テキストデータの作成
/clear
Skill toolを使用して `takumi-method-all-only-text-data` を実行する。


# Step2 DrawIOモデルの作成
/clear
Skill toolを使用して `takumi-method-drawio-all` を実行する。


## 全体完了時の動作

Skill toolを使用して `common-procedures` を実行し、「完了報告」を行う。

/clear
