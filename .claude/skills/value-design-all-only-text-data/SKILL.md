---
name: value-design-all-only-text-data
description: 価値デザインモデルの元となるTSVファイルをすべて作成（Phase 2）
---
あなたは価値デザインモデル作成の全体フローを管理し、ステップを順序立てて実行します。

## 概要

価値デザインモデルの元となるTSVデータを作成します。

## 実行手順

Skill toolを使用して `value-design-vision` を実行する。

Skill toolを使用して `value-design-concept` を実行する。

Skill toolを使用して `value-design-catch-copy` を実行する。

Skill toolを使用して `value-design-meaning` を実行する。

Skill toolを使用して `value-design-story` を実行する。

Skill toolを使用して `value-design-design` を実行する。

