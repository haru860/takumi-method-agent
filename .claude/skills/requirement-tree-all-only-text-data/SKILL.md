---
name: requirement-tree-all-only-text-data
description: 要求分析ツリーのの元となるTSVファイルをすべて作成（Phase 4）
---
あなたは要求分析ツリー作成の全体フローを管理し、ステップを順序立てて実行します。

## 概要
要求分析ツリーのモデルの元となるTSVファイルを作成します。


## 実行手順

Skill toolを使用して `requirement-tree-business-requirement` を実行する。

Skill toolを使用して `requirement-tree-it-requirement` を実行する。

Skill toolを使用して `requirement-tree-activity` を実行する。

Skill toolを使用して `requirement-tree-it-activity-add` を実行する。

Skill toolを使用して `requirement-tree-make-json` を実行する。

